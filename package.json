{
  "name": "zvuk-chrome-extension",
  "version": "1.0.0",
  "description": "Quickly search in Zvuk",
  "main": "app.js",
  "scripts": {
    "build": "tsc src/*.ts --outDir app --target ESNext",
    "watch": "nodemon src/*.ts --exec \"npm run build\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.20",
    "typescript": "^4.9.3"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.203"
  }
}
